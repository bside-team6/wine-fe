import { css } from '@emotion/react';

const Pagination = () => {
  return (
    <div
      css={css`
        margin-top: 100px;
        margin-bottom: 120px;
        display: flex;
        align-items: center;
        justify-content: center;
      `}
    >
      Pagination
    </div>
  );
};

export default Pagination;
